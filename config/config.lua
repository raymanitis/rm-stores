Config = {
	debug = false,
	fluctuatePrices = false,
	
	-- Framework Detection: 'QB' or 'QBOX' or 'auto' (auto-detect)
	Framework = 'auto',
	
	-- Inventory Detection: 'ox_inventory' or 'qb-inventory' or 'auto' (auto-detect)
	Inventory = 'auto',
	
	-- Target Detection: 'ox_target' or 'qb-target' or 'auto' (auto-detect)
	Target = 'auto',
}
